<!--
PR checklist (see CONTRIBUTING.md §3–§5):
  - Branch: `ricardo/{issue-or-topic}-{what-it-solves}` off `main`
  - Title:  `<TICKET> :: <one-line summary>`  (no emoji/gitmoji/verb prefix, imperative mood, ≤72 chars)
  - Assign yourself and add at least one label
  - `main` is fast-forward only — land via squash-merge (no merge commits)
  - Commit format: `<gitmoji> <Action>: <summary>` (e.g. `:sparkles: Add: ...`)
  - No Co-Authored-By / AI-attribution trailers
-->

## Summary

<!-- What changes and why, in one paragraph. -->

## Changes

<!-- Bulleted list of files / areas touched. -->

-

## Test plan

<!-- Explicit test commands, manual verification steps, and any out-of-band
     checks. Examples: `cargo test -q`, `pnpm lint`, `pytest --cov`. -->

- [ ] Tests pass locally
- [ ]

## Refs

<!-- Linear ticket links (`<TICKET>-<n>`), FR/NFR IDs, DEC-<slug> decision IDs.
     Use Closes: <TICKET>-<n> if this PR completes the ticket. -->

