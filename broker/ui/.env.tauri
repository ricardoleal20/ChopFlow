# Tauri build mode: the desktop app is served from a custom protocol origin, so
# the frontend must talk to the broker over an absolute URL instead of a
# relative "/api". Loaded when Vite builds with `--mode tauri`.
VITE_API_BASE=http://127.0.0.1:8080/api
